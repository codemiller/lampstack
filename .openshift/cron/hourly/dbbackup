#!/bin/bash

#DATE=`date +"%Y-%m-%d-%T"`
#FILE="$OPENSHIFT_APP_NAME-$DATE.sql.gz"
#INIT_PATH=$OPENSHIFT_DATA_DIR/$FILE
#BACKUP_DIR=$OPENSHIFT_DATA_DIR/mongobackup

#if [ ! -d "$BACKUP_DIR" ]; then
#  mkdir $BACKUP_DIR
#fi
#mongodump -u $OPENSHIFT_MONGODB_DB_USERNAME -p $OPENSHIFT_MONGODB_DB_PASSWORD -h $OPENSHIFT_MONGODB_DB_HOST -d $OPENSHIFT_APP_NAME --port $OPENSHIFT_MONGODB_DB_PORT --out $OPENSHIFT_APP_NAME | gzip > $INIT_PATH
#mv $INIT_PATH $BACKUP_DIR/$FILE
